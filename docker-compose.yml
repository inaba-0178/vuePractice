version: '3.8'

services:
  vue-app:
    build: .
    container_name: vue-dev
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/public
    environment:
      - NODE_ENV=development
    stdin_open: true
    tty: true